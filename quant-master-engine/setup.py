from setuptools import setup, find_packages

setup(
    name="quant-master-engine",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are managed in requirements.txt
    ],
    author="Antigravity",
    description="Unified Quant Engine Architecture for advanced trading and analysis.",
    python_requires=">=3.9",
)
